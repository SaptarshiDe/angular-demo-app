export class Ingredient{

  /*  public name: string;
    public amount: number;

    constructor(name: string, amount: number){
        this.name = name;
        this.amount = amount;
    }

    While declaring a model class, we can take either of the two approaches if we decide to pass all the parameters
    via constructor.
    */

    constructor(public name: string, public amount: number){

    }
}